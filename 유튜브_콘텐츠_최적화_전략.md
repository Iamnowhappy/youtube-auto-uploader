# 유튜브 콘텐츠 최적화 전략 (자동화 프로젝트 생성/수정 시 항상 먼저 참고)

> Cowork는 채팅창이 바뀌면 이전 대화를 기억하지 못한다. 유튜브 영상을 만들거나
> 업로드하는 **어떤 자동화 프로젝트**(대본 생성, 이미지 생성, TTS, 영상 합성,
> 업로드, 시트 연동 등 무엇이든)를 새로 만들거나 수정하는 채팅을 시작할 때는
> 이 파일 내용을 대화 맨 앞에 붙여넣거나, 이 파일을 먼저 읽게 할 것.
>
> 이 문서는 "유튜브 업로드 단계"에만 국한되지 않는다 — 기획/대본/이미지/TTS/
> 업로드/사후관리까지 콘텐츠 파이프라인 전체에 적용되는 전략이다. 새 자동화
> 프로젝트를 만들 때는 이 규칙들을 **문서로만 남기지 말고 실제 코드에 구현**할 것.
>
> 사본 위치: `30_YouTube_View_Optimization`(마스터) / `29_japan_senior_story_longform`
> / `5_youtube_auto_uploader`. 한 곳을 고치면 나머지 두 곳도 동일하게 갱신할 것.

---

## 1. 핵심 원칙 (기획 단계에서 항상 먼저 확인)

1. **지표는 거짓말을 한다.** 클릭률·시청 지속 시간이 좋아도 조회수가 낮으면
   "충성 구독자에게만 소비된 것"일 수 있다 — 실패로 단정하지 말 것. 반대로
   지표가 나빠도 트렌드를 탄 영상이 훨씬 크게 확산되는 경우가 많다.
2. **트렌드가 소재보다 우선한다.** 채널이 작으면 구글 트렌드(탐색 탭, 최근 1개월,
   유튜브 검색 필터)로, 채널이 어느 정도 크면 스튜디오 분석 > 트렌드 탭으로
   먼저 확인하고 주제를 정할 것.
3. **문제 중심 포지셔닝 > 해결책 중심 포지셔닝.** "해결책"을 제목/기획에 먼저
   내세우면 이미 문제를 인지한 소수만 유입된다. "문제"를 먼저 언급하면(예:
   "무릎 안 아프게 스쿼트하는 법" 대신 "무릎이 아픈 이유") 잠재 시청자 전체로
   범위가 넓어지고, 해결책은 영상 안에서 전달하면 된다.
4. **결말/반전을 제목에서 다 보여주지 말 것.** 시청자가 이미 답을 아는 순간
   후킹이 사라진다. 2인칭 질문형 훅("당신은 가족 중 자신만 손해 본다고 느낀
   적 없습니까?")이 결과를 다 말하는 제목보다 넓은 시청자를 끈다.

## 2. 대본/기획 단계에 반드시 넣어야 하는 것

새 대본 생성 프롬프트(`prompts.py` 류)를 만들거나 고칠 때 아래를 기본 구조로
유지할 것 (이미 27~29번 프로젝트의 `SCRIPT_TEXT_PROMPT`에 반영돼 있음 — 새
프로젝트를 포크/신규 생성할 때도 그대로 이식할 것):

- 아이디어 검증 4문항: ①채널 기존 주제와 일치하는가 ②해결책이 아니라 문제를
  언급하는가 ③현재 트렌드 주제인가 ④개인 소식/자기만족용은 아닌가.
- 훅 구조: 영상 시작 3초 내 후킹(인사말 생략), 30초 이내 핵심 키워드를 육성
  나레이션에 직접 포함(유튜브가 오디오를 텍스트로 변환해 내용을 파악하므로).
- 2인칭 질문 훅 + 결말 방향을 흐릿하게 예고(두 가능성만 제시).
- 반전 패턴: 뒤집기형(패턴A) 또는 축적형(패턴B, 이전 사실을 부정하지 않고
  새 사실을 계속 얹어 층층이 악화) 중 소재에 맞게 선택.
- 챕터 표시 관행: "第◯章 소제목"을 낭독체 문장에 자연스럽게 섞어 넣기 — 이건
  화면 챕터 타임스탬프와 별개로 대본 자체의 구조화 장치. 챕터 끝마다 사건의
  의미를 한 문장으로 요약하는 내레이션도 추가.
- 톤 로테이션: 감동눈물/훈훈함/코미디/막장 등 여러 톤을 두고 연속 편성 시
  최근 안 쓴 톤을 우선 배정(`theme_picker.pick_tone()` 패턴). 톤별로 전용
  해시태그를 매핑해둘 것(`TONE_PRESETS`에 `hashtag` 필드 — 아래 4번 참고).

## 3. 업로드 자동화 코드가 반드시 구현해야 하는 것

`upload.py` / `finalize.py` 류 스크립트를 새로 만들거나 고칠 때 체크리스트:

- **영상 길이 실측(ffprobe)으로 숏츠/롱폼 판정.** 무조건 "shorts" 태그를
  넣지 말고, 실제 길이가 3분(180초) 이하일 때만 숏츠로 취급. (이미 이
  프로젝트의 `upload.py`에 반영됨 — 새 업로더를 또 만들 경우 반드시 이식할
  것. 이 판정을 빼먹으면 롱폼에 "#shorts"가 붙어 조회수가 치명적으로
  저조해지는 사고가 실제로 발생했다.)
- **defaultLanguage / defaultAudioLanguage를 콘텐츠 실제 언어로.** 채널별
  매핑 테이블을 두고, 대본이 일본어면 "ja", 한국어면 "ko" 등 실제 언어로
  명시. "제목 및 설명 언어"가 실제 언어와 다르면 검색/추천에서 불리하다.
- **설명란 첫 두 문장에 핵심 키워드를 배치.** 스크립트 요약을 간결하게 넣고,
  키워드는 반드시 앞 두 문장 안에 있어야 한다(뒤에 있으면 늦게 읽혀 효과가
  떨어짐).
- **챕터는 사람이 눈대중으로 만들지 말고 실제 컷 데이터로 자동 계산할 것.**
  ✅ 2026-07-26 구현 완료(29번 프로젝트): `senior_longform_video.py`가
  whisper 분석 직후 `storage.build_youtube_chapters_text()`로 컷별 실제
  발화 시간을 기준으로 챕터를 계산해 `youtube_chapters.txt`로 저장하고,
  `senior_longform_finalize.py`가 이를 읽어 `storage.
  append_chapters_to_sheet_description()`로 시트 B열(설명란)에 직접
  이어붙인다 — 즉 29번 파이프라인에서 오는 영상은 이 프로젝트의 O열
  (chapters_raw)이 아니라 B열에 챕터가 이미 포함된 채로 들어온다. 규칙
  (0:00 시작/최소 3개/10초 이상 간격)은 `build_youtube_chapters_text()`가
  항상 강제한다.
  ⚠️ 단, 대본 프롬프트가 "第◯章、소제목" 뒤에 곧바로 마침표(。)를 찍도록
  명시하지 않으면, 소제목이 다음 문장까지 물고 들어가 챕터 라벨이 깨지는
  버그가 있다(실제 cuts.json으로 재현 확인). 새 콘텐츠 생성 프로젝트를 만들
  때 `prompts.py`의 챕터 지침에 "소제목 직후 반드시 。로 끊을 것"을 반드시
  포함시킬 것.
- **톤별 해시태그를 설명란에 자동 삽입.** `BASE_HASHTAGS`(장르 고정) +
  `TONE_PRESETS[tone].hashtag`를 조합해서 붙일 것 — `upload.py`가 description
  안의 "#단어"를 그대로 태그로도 추출하는 기존 로직을 그대로 재사용하면
  태그와 해시태그가 동시에 채워진다.
- **재생목록 자동 배정.** ✅ 2026-07-26 구현 완료(29번): `storage.
  build_playlist_name()`/`build_playlist_description()`이 시트 P/Q열에
  자동으로 채워지고, 이 프로젝트의 `playlist_ops.py`가 그 값을 읽어 재생목록
  생성/재사용/추가를 처리한다. 지금은 테마별 세분화 없이 채널/장르 단위
  고정 재생목록("家族の秘密 感動ストーリー") 하나로 통일.
- **AI 사용(합성 콘텐츠) 공개를 업로드 시점에 API로 자동 선언.** ✅
  2026-07-30 구현 완료: `upload.py`가 `status.containsSyntheticMedia`(bool)
  를 `CHANNEL_SYNTHETIC_MEDIA_MAP`(채널 기본값) + S열(영상별 선택 오버라이드)
  로 계산해 `videos.insert`/`update`에 함께 보낸다. **사실적인 인물/장면을
  AI로 생성·변경한 채널만** True로 등록할 것 — 단순 TTS 내레이션이나
  일러스트풍 이미지는 disclosure 대상이 아니므로 전 채널에 무조건 True를
  넣지 말 것.
- **고정 댓글 후보 문구 자동 생성.** ✅ 2026-07-26 구현 완료(29번): `storage.
  build_pinned_comment(title)`이 영상 제목을 인용한 질문형 문구를 만들어
  R열에 채우고, 이 프로젝트의 `comment_ops.py`가 그 값을 읽어 댓글 작성까지
  자동화한다. YouTube Data API에는 댓글 "고정(pin)" 엔드포인트가 없으므로
  고정은 항상 사람이 스튜디오에서 직접 해야 한다.
- **채널 워터마크(구독 유도 아이콘) 자동 등록.** ✅ 2026-07-30 구현 완료:
  `watermark_ops.py`(등록/제거 함수) + `channel_watermark_setup.py`(채널당
  1회만 로컬에서 직접 실행하는 CLI, 자동 업로드 파이프라인과 분리)로
  `watermarks.set` API를 사용해 채널에 워터마크 이미지를 등록한다.
  `youtube.upload` 스코프만으로도 동작(공식 문서 확인) — 재생목록/댓글과
  달리 스코프 재발급 불필요. 사용법은 이 프로젝트 `README.md`의 "채널
  워터마크 설정" 섹션 참고.

## 4. 자동화 불가능한 것 (API 공식 한계 — 2026년 기준 확인 완료)

아래는 아무리 코드를 잘 짜도 YouTube Data API v3로 자동화할 수 없다. 코드는
이 항목들을 콘솔 로그로 "수동 처리 필요" 안내만 출력하도록 설계할 것(이미
`upload.py`가 재생목록/댓글 실패 시 경고 로그를 출력하도록 되어 있음):

- 엔드 스크린(End screen) / 카드(Cards) 등록·수정
- 썸네일·제목 A/B 테스트 등록
- 댓글 "고정(pin)" (작성은 가능, 고정만 불가)
- 채널 자동 챕터(자동 챕터 및 주요 순간 허용) on/off 토글
- 쇼츠 리믹스 허용 토글 (공식 문서에서 전용 필드를 찾지 못함 — "미확인"에
  가까움, 확인되면 이 문서 갱신할 것)

> ⚠️ **정정(2026-07-30)**: 이전에 이 목록에 "워터마크 등록(채널 고급 설정
> UI 전용)"이 있었는데 틀린 정보였다. `watermarks.set`/`watermarks.unset`
> API가 실제로 존재하며 `youtube.upload` 스코프만으로도 동작한다(공식 문서
> `developers.google.com/youtube/v3/docs/watermarks/set` 확인). 위 3번
> 섹션의 "채널 워터마크 자동 등록" 항목과 8번 섹션 참고.

## 5. 업로드 직후 사람이 스튜디오에서 직접 할 일 (자동화 코드가 안내만 할 것)

1. 엔딩 화면에 채널 인기 영상 1개 연결.
2. 방금 작성된 고정 댓글 후보를 상단 고정.
3. 제목/썸네일 3안 A/B 테스트 등록.
4. 분석 > 도달범위 탭에서 CTR 확인(5% 이하면 제목/썸네일 즉시 교체).
5. 시청 지속 시간 그래프에서 초반 이탈률이 크면 편집기로 오프닝 컷.

## 6. 죽은 영상(업로드 48시간 후 조회수 정체) 소생 절차

조건: `업로드 후 48시간 경과 AND 조회수 그래프가 평행선`.

1. 설명란에 키워드 포함 챕터 추가(위 3번 규칙 그대로).
2. 재생목록 생성/배치 + 재생목록 설명에 키워드.
3. 채널 인기 영상 엔딩 화면에 연결.
4. 키워드 유도형 고정 댓글 작성(단순 시청 요청 금지).
5. CTR 확인 → 5% 이하면 제목/썸네일 교체(시의성 키워드 추가: "2026년", 계절 등).
6. 설명란/태그 재작성(첫 문장 키워드, 태그 도구로 5개 재선정).
7. 초반 이탈률이 크면 편집기로 오프닝 컷.

⚠️ **이미 업로드된 영상은 코드를 아무리 고쳐도 소급 적용되지 않는다.** `upload.py`를
개선하는 것과, 이미 올라간 특정 영상을 살리는 것은 완전히 별개 작업이다 —
후자는 항상 스튜디오에서 수동으로 처리해야 한다.

## 7. 실제 사례로 배운 교훈 (회고)

- **shorts 하드코딩 사고**: `upload.py`가 영상 길이·채널과 무관하게 항상
  tags에 "shorts"를 넣고 description에 "#shorts"를 붙이던 로직이 16분대
  롱폼(29번 프로젝트)에도 그대로 적용되어, 업로드 12시간이 지나도 조회수
  1에 머무는 원인이 됐다. → ffprobe 실측 판정으로 수정.
- **진짜 해시태그 부재**: 채널이 지금까지 설명란에 "#shorts" 외의 진짜
  해시태그를 넣은 적이 한 번도 없었다는 사실이 뒤늦게 발견됨. → 29번
  프로젝트에 톤별 해시태그 자동 삽입 추가, 이 프로젝트는 그 값을 그대로
  태그로 추출.
- **챕터 부재 반복**: 두 편 연속으로 롱폼 영상에 챕터 타임스탬프가 전혀
  없이 업로드됨(설명란이 그냥 스토리 요약 프로즈뿐) — 매번 사람이 사후에
  cuts.json을 열어 눈대중으로 챕터를 계산해야 했다. → 이 문서 3번의 "챕터
  자동계산"을 다음 자동화 우선 과제로 명시. 이 프로젝트의 `chapters.py`는
  이미 I열 값을 검증/삽입할 준비가 돼 있으나, I열을 자동으로 채워주는 쪽
  (29번)이 아직 없다.
- **defaultLanguage 하드코딩**: 전 채널이 "ko"로 고정돼 있어 일본어 콘텐츠
  채널(채널7)에 잘못된 값이 붙어 있었다. → 채널별 언어 매핑 테이블로 수정.
- **2026-07-26 챕터/재생목록/고정댓글 자동화 추가**: `chapters.py` /
  `playlist_ops.py` / `comment_ops.py` 신설, 구글 시트 I/J/K/L열(전부 선택
  입력)로 연결(자세한 내용은 이 프로젝트 `README.md` 변경 이력 참고). 다만
  이 코드는 **업로드 시점 이후에만 적용**되므로, 이미 올라간 영상에는
  소급 적용되지 않는다는 점을 실제로 확인함(같은 날 업로드된 영상이
  코드 반영 전 상태로 조회수 1에 머무는 것을 사용자가 재확인).
- **여러 프로젝트가 같은 시트를 공유할 때 컬럼 충돌 사고 위험**: 위 항목의
  I/J/K/L열이 27/28/29번이 이미 쓰고 있던 상태/수집일시/테마ID/생성ID
  자리와 정면으로 겹친다는 걸 뒤늦게 발견 — 그대로 뒀다면 생성ID 같은
  숫자가 "고정 댓글 문구"로 잘못 읽혀 실제 영상에 의미 없는 댓글이 달릴
  뻔했다. → 실행 전에 발견해서 O/P/Q/R열로 이동(자세한 내용은 이 프로젝트
  README.md 2차 변경 이력 참고). **여러 프로젝트가 공유하는 시트에 새 컬럼을
  추가하기 전에는 반드시 그 시트를 쓰는 다른 프로젝트들의 `config.py` 컬럼
  상수를 먼저 확인할 것.**
- **챕터 소제목 뒤에 마침표가 없으면 라벨 추출이 다음 문장까지 물고
  들어간다**: 29번 프로젝트의 실제 cuts.json으로 재현 확인. → 대본 생성
  프롬프트에 "소제목 직후 반드시 。로 끊을 것"을 명시하는 규칙을 반드시
  포함시킬 것.
- **AI 사용 공개를 API로 선언하지 않으면 유튜브가 자체 감지로 뒤늦게 라벨을
  붙인다**: 자동 업로드 과정에 아무도 스튜디오의 "AI 사용" 질문에 답을
  넣지 않았더니, 일부 영상에만 유튜브 자체 판단으로 AI 라벨이 붙어 있는
  걸 사용자가 우연히 발견함. → `upload.py`가 업로드 시점에 `status.
  containsSyntheticMedia`를 API로 직접 선언하도록 수정. 다만 사실적인
  AI 인물/장면을 쓰는 채널에만 한정할 것(전 채널 일괄 True는 과잉 신고).
- **"API 미지원"이라고 적어둔 항목도 주기적으로 재검증할 것**: 워터마크
  등록을 이 문서 4번에 API 미지원이라고 적어뒀었는데, 실제로는
  `watermarks.set` 엔드포인트가 존재해서 자동화가 가능했다(공식 문서로
  뒤늦게 확인). 자동화 불가 판정은 항상 공식 문서 링크와 함께 남기고, 새
  유튜브 성장 강의/아티클을 참고할 때마다 이 문서의 "불가능" 목록도 같이
  재확인할 것.

## 8. 채널 최초 1회 설정 체크리스트 (자동화 코드와 별개, 채널당 딱 한 번)

2026-07-30에 유튜브 성장 강의(업로드 세팅 18가지) 대본을 항목별로 대조해서
정리함. 아래는 "매 영상마다"가 아니라 "채널 하나 만들 때 딱 한 번"만 사람이
확인하면 되는 항목들이다 — 자동 업로드 코드가 매번 대신 해주는 영역이
아니므로, 새 채널을 만들 때마다 이 체크리스트를 스튜디오에서 직접 확인할 것.

**계정/보안 설정 (스튜디오에서 사람이 1회 직접 설정, 자동화 대상 아님)**
- [ ] 채널 키워드 등록 (설정 > 채널) — 주제가 섞이지 않게 일관된 키워드로.
- [ ] 채널 설명 첫 문장에 핵심 키워드 포함 (맞춤설정).
- [ ] 비즈니스 문의 이메일 등록 — 현재 쓰는 개인 이메일 말고 별도로 새로
      만들 것(해킹 위협 대비).
- [ ] 기능 사용 자격요건(중급 전화인증/고급) 인증 완료.
- [ ] 구독 정보 모두 비공개 설정 (계정 > 공개 범위 설정).
- [ ] 2단계 인증 (구글 계정 보안).
- [ ] 업로드 기본값 설정 — 기본 공개 상태를 비공개/일부공개로(실수로 바로
      공개되는 사고 방지).

**API로 자동화 완료 (이 프로젝트에 이미 구현됨 — 위 3번 섹션 참고)**
- [x] 영상 카테고리 지정 (`categoryId`).
- [x] 재생목록 자동 배정.
- [x] 챕터 자동 계산 + 설명란 타임스탬프 삽입(자동 챕터 기능에 의존하지
      않고 직접 텍스트로 넣는 방식 — 강의에서 권장하는 "자동 챕터 끄고
      수동 입력" 원칙과 정확히 일치).
- [x] 설명란 SEO 원칙(첫 문장 키워드, 스크립트 요약, 해시태그).
- [x] 고정 댓글 후보 문구 자동 작성(고정 자체는 API 미지원, 위 4번 참고).
- [x] AI 사용(합성 콘텐츠) 공개 자동 선언.
- [x] 채널 워터마크(구독 유도 아이콘) 자동 등록 — `channel_watermark_setup.py`
      로 채널당 1회만 실행.

**API 미지원 확정 — 항상 스튜디오에서 수동 처리 (위 4번 참고)**
- [ ] 엔드 스크린에 연관 영상 1개 연결.
- [ ] 썸네일·제목 3안 A/B 테스트 등록.
- [ ] 자동 챕터 및 주요 순간 허용 체크 해제.
- [ ] 쇼츠 리믹스 허용 여부(미확인 — 필요시 재검토).

**자동화 대상이 아니라 "원칙/카피라이팅" 영역 (코드가 아니라 대본/제목
생성 프롬프트에 이미 반영돼 있거나, 반영돼 있는지 새 프로젝트마다 확인할 것)**
- [x] 제목에 궁금증+주제+핵심 키워드 3요소 포함 (위 2번 섹션 원칙과 동일선상).
- [x] 설명란 첫 두 문장에 핵심 키워드 (위 3번 섹션에 이미 있음).

---

이 문서는 새 항목이 생기면 위 섹션에 이어서 추가하고, "7. 실제 사례로 배운
교훈"에 날짜 없이도 좋으니 계속 누적할 것(기존 내용을 지우지 말 것).
